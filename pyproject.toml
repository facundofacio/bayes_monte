[project]
name = "bayes_monte"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "bambi==0.13.0",
    "kulprit==0.0.1",
    "nutpie",
    "preliz==0.3.6",
    "pymc-bart==0.5.2"
]

[tool.uv]